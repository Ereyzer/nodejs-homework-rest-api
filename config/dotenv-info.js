require("dotenv").config();
//* this file we need only for take off "require("dotenv").config();"
//* from all another files and we don't have to repeat this import again

module.exports = process.env;
